#include<iostream>
using namespace std;
int main()
{
    long long t;
    for(int i=0;i<t;i++)
    {
        long long n,k;
        cin>n;
        cin>>k;
        long long corr[k];
        for(p=1;p<=k;p++)
        {
            corr[p]=p;
        }
        for(int a=0;<n;a++)
        {
            cin>>first;
            for(int x=0;x<first;x++)
            {
                cin>>d[x];
                for(b=0;b<k;b++)
                {
                    if(corr[b]==d[x])
                        corr[b]=1;
                }
            }
        }
    }
}
